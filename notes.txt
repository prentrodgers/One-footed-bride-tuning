For Future Updates
If you make changes to any file in the repo, here's how to sync them:

# 0. From your ~/Repos/One-footed-bride-tuning directory
    cd ~/Repos/One-footed-bride-tuning

# 1. Check what's changed
    git status

# 2. Add the modified file
    git add One-Footed-Bride-Presentation.md
or just add everything that's changed:
    git add .

# 3. Commit with a message
    git commit -m "Improve grid-search - include different tonality diamond limits"

# 4. Push to GitHub
    git push origin main

# 5. Pull in any other clones (if you have them elsewhere)
git pull origin main


I should check if the max_no_improve=50 compares scores or actual cent tuning with np.array_equal(old, new). It should do that, since if it's still shifting things around with different cent values, I think I should keep going. 
It does something entirely different. It just checks how many times in a row there is no improvement, not even looking at the score, much less that all the non-improving results were identical. But I set it pretty high at a default value of 50. What happens if you increase it. 